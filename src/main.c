/**
 * Main file for cproc - parses command-line arguments and executes the main
 * preprocessor code.
 */
#include <stdio.h>
#include <display.h>

int main(int argc, char** argv) {
	cp_version();
    return 0;
}
