/**
 * Main file for cproc - parses command-line arguments and executes the main
 * preprocessor code.
 */
#include <stdio.h>
#include <display.h>

void usage() {
	printf(usage_msg);
}

int main(int argc, char** argv) {
    return 0;
}
